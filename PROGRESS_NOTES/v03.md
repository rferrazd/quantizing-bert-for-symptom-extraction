# V03: BioBERT symptom NER (expanded synthetic data)

Goal for this version was **not** to chase leaderboard token-F1 alone, but to address failure modes seen on a behavioural set in v02 by **fixing the training distribution**: many more sentence templates, explicit **negative / distractor** contexts (symptom *words* that must **not** be tagged as entities), and harder boundaries. Implementation lives in [`v03/dataset_templates.py`](../v03/dataset_templates.py) 
  > 41 `NEGATED_TEMPLATES`
  > 40 `AFFIRMED_TEMPLATES` 
  > 38 `DISTRACTOR_TEMPLATES`
  > total of 109 templates


Baseline plan from [v02 notes](./v02.md): keep **full fine-tuning** and invest in corpus diversity rather than swapping training style for the boundary-overreach case.

---

## Dataset

- **Hub:** `Rogarcia18/symptoms_ner_v03_biobert`
- **Templates:** expanded affirmed and negated phrasings; **distractor** lines use `{SYMPTOM}` in medical or administrative context but are labeled **all `O`** (no `SYMPTOM_POS` / `SYMPTOM_NEG` span), so the model learns *when not* to extract.
- If you add more “unpolarized” rows later, keep the contract explicit in code/comments: either **all-`O` distractors** or real **POS/NEG** spans—mixing unlabeled gold inside otherwise supervised rows will silently hurt training.
- **Size of the dataset:** 106267 rows (80% training, 10% val, 10% test)


---

## Training

### First attempt (stopped early)

- **Model:** `dmis-lab/biobert-base-cased-v1.1`
- **Config intent:** lr=5e-5, batch_size=16, epochs=20 (run index 2 from `biobert_hyperparams`)
- **WandB:** https://wandb.ai/robdallagogar-ferraz-h/symptom-ner/runs/syq4zv8c
- **HF branch:** `run-2-biobert_base_cased_v1_1-lr5e05-bs16-ep20`

| Epoch | train_loss | eval_loss   | eval_f1 | eval_precision | eval_recall |
|-------|-----------|-------------|---------|----------------|-------------|
| 1     | 0.09184   | 2.856e-05   | 1.0000  | 1.0000         | 1.0000      |
| 2     | 0.003121  | 6.311e-05   | 0.9999  | 1.0000         | 0.9999      |
| 3     | 0.003341  | 8.961e-04   | 0.9984  | 0.9983         | 0.9985      |
| 4     | 0.002432  | 9.489e-04   | 0.9983  | 0.9979         | 0.9988      |

**Why stopped:** `eval_loss` rose after epoch 1 while micro-F1 barely moved—classic overfitting signal. Run was cut to save credits; **`hub_strategy="end"`** meant **nothing was pushed to the Hub or GCS** when the job was cancelled.

### Completed run (2 epochs — production checkpoint)

Matches the regime above but **two epochs**, then Hub push + artefact sync. Local mirror:

- **`v03/downloaded_models/dmis-lab/biobert-base-cased-v1.1/run_2/`** — tokenizer + weights + [`summary.json`](../v03/downloaded_models/dmis-lab/biobert-base-cased-v1.1/run_2/summary.json)

**Hyperparameters**

| Setting        | Value |
|----------------|-------|
| `model_name`   | `dmis-lab/biobert-base-cased-v1.1` |
| `dataset_repo` | `Rogarcia18/symptoms_ner_v03_biobert` |
| `epoch`        | 2 |
| `lr`           | 5e-05 |
| `batch_size`   | 16 |
| `weight_decay` | 0.01 |
| `warmup_ratio` | 0.1 |
| `push_to_hub`  | true |

**Approx. training wall time:** ~14.6 minutes (`summary.json`).

---

## Held-out metrics (`summary.json`)

Micro-averaged token metrics:

| Split        | Precision | Recall | F1     | Accuracy  |
|-------------|-----------|--------|--------|-----------|
| Validation  | 0.999862  | 0.999862 | 0.999862 | 0.999983 |
| Test        | 0.999722  | 0.999722 | 0.999722 | 0.999966 |

Per-entity validation (counts are support strings from [`val_metrics.json`](../v03/downloaded_models/dmis-lab/biobert-base-cased-v1.1/run_2/val_metrics.json)):

| Label         | Precision | Recall | F1       | Examples |
|--------------|-----------|--------|----------|----------|
| `SYMPTOM_NEG` | 0.999732 | 0.999732 | 0.999732 | 3733 |
| `SYMPTOM_POS` | 1.000000 | 1.000000 | 1.000000 | 3514 |

```json
{
  "model_name": "dmis-lab/biobert-base-cased-v1.1",
  "training_time_minutes": 14.57,
  "hyperparameters": {
    "model_name": "dmis-lab/biobert-base-cased-v1.1",
    "dataset_repo": "Rogarcia18/symptoms_ner_v03_biobert",
    "epoch": 2,
    "lr": 5e-05,
    "batch_size": 16,
    "weight_decay": 0.01,
    "warmup_ratio": 0.1,
    "push_to_hub": true
  },
  "validation_metrics": {
    "f1": 0.9998620118669794,
    "precision": 0.9998620118669794,
    "recall": 0.9998620118669794,
    "accuracy": 0.9999831016856069
  },
  "test_metrics": {
    "f1": 0.9997224920216456,
    "precision": 0.9997224920216456,
    "recall": 0.9997224920216456,
    "accuracy": 0.9999662561160789
  }
}
```

**Caveat:** validation/test F1 this high can coexist with weaker **behavioural-set** behaviour (distribution shift and span semantics). Treat the behavioural pass as the product-facing sanity check.

---

## Behavioural set (error taxonomy)

Notebook: [`v03/9_categorize_errors_in_behavoural_set.ipynb`](../v03/9_categorize_errors_in_behavoural_set.ipynb) — loads `run_2`, runs `predict_word_level` / `word_labels_to_spans`, then error categorization on the behavioural suite.

Aggregate error counts (**46** total) with `dmis-lab/biobert-base-cased-v1.1` `run_2`:

```json
{
  "Boundary Overreach": 18,
  "Irrelevant Span Mislabeling": 9,
  "Missing Expected Entity": 8,
  "Model Overgeneralization": 7,
  "Incorrect Polarity Assignment": 4
}
```


---


CLAUDE, do not populate below.
# try the same but with only training the head (V03.1)


## Results


### Summary 
```json

```

### Behavioural Set

```json

```
